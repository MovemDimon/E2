{
  "name": "frontend-repo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run serve",
    "serve": "vue-cli-service serve",     
    "build": "vue-cli-service build",      
    "test:unit": "jest --coverage",
    "test:e2e": "cypress run --record --key your-key-if-needed"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0",
    "@babel/preset-env": "^7.20.0",
    "jest-environment-jsdom": "^29.0.0",
    "babel-jest": "^29.0.0",
    "cypress": "^12.0.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
  }
}
