{
  "name": "frontend-repo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "vue-cli-service serve --port 8080 --host 0.0.0.0",
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "test:unit": "jest --coverage --passWithNoTests",
    "test:e2e": "start-server-and-test start http://localhost:8080 cypress:run",
    "cypress:run": "cypress run"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0",
    "@babel/preset-env": "^7.20.0",
    "babel-jest": "^29.0.0",
    "start-server-and-test": "^2.0.0", 
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0",
    "cypress": "^12.0.0"
  },
  "dependencies": {}
}
